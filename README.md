# atolli-dl-setup
Scripts for VM installations when environment is open 
1. Install general software
2. Install/update Python
3. Create `venv`
4. Install Python packages into venv from `pre_requirements.txt`
5. Export actual requirements from the venv into `post_requirements.txt`
6. Create `wheelhouse` for future flexibility from `post_requirements.txt`
7. 